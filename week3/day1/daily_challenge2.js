// Write a JavaScript program that recreates the pattern below.
let star="";
for (let i = 0; i < 6; i++) {
    star += '*' ;

    console.log(star);
}

// Do this challenge using two nested for loops (Nested means one inside the other - check out this tutorial of nested loops).

